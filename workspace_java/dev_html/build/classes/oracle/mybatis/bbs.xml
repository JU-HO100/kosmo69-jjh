<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="oracle.mybatis.BbsMapper">
	<select id="getBbsList" resultType="map">
		SELECT b_num, b_title, b_mem, b_hits, b_day  
		  FROM boardlist
		  ORDER BY b_num DESC
	</select>
	<insert id="insertBbs" parameterType="web.mvc.BbsVO">
			insert into boardlist(b_num, b_title, b_day, b_hits, b_mem, b_pw, b_contente)
			values(boardlist_seq.nextval,#{B_TITLE},TO_CHAR(SYSDATE,'YYYY-MM-DD'),#{B_HITS},#{B_MEM},#{B_PW},#{B_CONTENTE})
		</insert>
		
	<delete id="deleteBbs" parameterType="map">
		DELETE b_num FROM boardlist
	</delete>
<!-- 	<update id="updateBbs"> -->
		
<!-- 	</update> -->
	<select id="getContente" parameterType="map" resultType="map">
		SELECT b_title, b_mem, b_hits, b_day, b_contente 
		  FROM boardlist
		  <where>
		  	<if test="b_num != null and b_num.length>0">
		  		and b_num = #{b_num}
		  	</if>
		  </where>
	</select>
</mapper>
<!-- 현재 1층에 있는 이디야에 가려면 엘리베이터를 타고 가야하는대 그중 1층의 이디야에 갈수만 있다면 
	아무거나 타도 상관없다
  -->
